#include <stdio.h>

int main(){
    
    char a = 'e';//Le asigno una variable de tipo char a la "a" siendo a = e.
    
    /*
    EL char es para que tome un solo valor, este sra un simbolo (a, 1, +)
    */
    printf("El elemento es: %c" ,a);//Le digo que imprima lo que esta en la variable "a".

    return 0;
}